{
  "version": 2,
  "env": {
    "CI": "false"
  },
  "builds": [
    {
      "src": "billion-hopes/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build",
        "framework": null
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}